repos:
  - repo: local
    hooks:
      - id: ci-gate
        name: lint/format fix + typecheck + tests
        entry: bash scripts/pre-commit.sh
        language: system
        pass_filenames: false
        always_run: true
