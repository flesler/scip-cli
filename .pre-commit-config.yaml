repos:
  - repo: local
    hooks:
      - id: ci-gate
        name: CI gate (scripts/test.sh)
        entry: bash scripts/test.sh
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
